"use strict";

/*

Write a recursive function that computes the nth Fibonacci number, where nth is an argument passed to the function.

input:  fib index
return: element at that index

Rules:
1. Use Recursion
    A. Function calls itself at least once.
    B. It has an ending condition.
    C. The result of each recursion are used in the next step.
2. Fib sequence follows Fn = (Fn - 1) + (Fn - 2)

Algorithm:
1. 

*/

const fibonacci = function fibonacci(idx) {

};

fibonacci(1);       // 1
fibonacci(2);       // 1
fibonacci(3);       // 2
fibonacci(4);       // 3
fibonacci(5);       // 5
fibonacci(12);      // 144
fibonacci(20);      // 6765
